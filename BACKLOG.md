# Backlog

## Деинсталлятор: гранулярный выбор что удалять

Сейчас `InitializeUninstall` задаёт один общий вопрос «удалить модели и записи?».
Нужно окно с отдельными чекбоксами:
- [ ] скачанные модели распознавания (`models_dir`)
- [ ] Ollama и модель сводок (отдельно: она ставилась через winget, живёт в `%USERPROFILE%\.ollama`)
- [ ] записи и расшифровки (`recordings_dir`)
- [ ] конфиг (`~/.config/amanu/config.json`)

По умолчанию всё снято (ничего не удалять). Silent-деинсталляция не спрашивает
и ничего не удаляет (уже сделано). Реализация: `InitializeUninstall` не умеет
рисовать чекбоксы — нужна кастомная страница через `UninstallProgressForm`
или отдельная форма на `CreateCustomForm` в `InitializeUninstall`.

Статус: не начато.
